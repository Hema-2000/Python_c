#include<stdio.h>
int main()
{
	int a,b,result;
	printf("enter a and b values");
	scanf("%d %d",&a,&b);
	result=a+b;
	result=a-b;//arithmetic operators
	result=a/b;
	result=a%b;
	result=a&b;
	result=a|b;//bitwise operators
	result=a^b;
	result=a<<b;
	result=a>>b;
	if(a>b)//relative operators
	printf()
	else
	printf()	
	if(a<b)
	if(a<=b)
	if(a>=b)
	if(a==b)
	if(a>b&&a<b)//logical operator
	printf()
	else
	printf()
	if(a<b||a>b)
	if(!a)	
		
